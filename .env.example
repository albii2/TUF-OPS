# This file is an example. Create a local .env file with your actual secrets.
# You can generate a secure secret with: openssl rand -base64 32

# NextAuth.js
# A secret is required for production. It's used to sign cookies and tokens.
NEXTAUTH_SECRET="changeme"
NEXTAUTH_URL="http://localhost:3000"

# Prisma
# Your database connection string.
DATABASE_URL="postgresql://user:password@localhost:5432/tuf?schema=public"
