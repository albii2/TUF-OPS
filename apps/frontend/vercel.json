{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "installCommand": "cd ../.. && corepack enable && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../.. && VITE_ENABLE_LEAD_BOOTSTRAP=true pnpm --filter web build && rm -rf apps/frontend/dist && cp -R apps/web/dist apps/frontend/dist",
  "outputDirectory": "dist"
}
