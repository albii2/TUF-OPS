{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm --filter @apps/api build && pnpm --filter web build && mkdir -p apps/api/dist/public && cp -r apps/web/dist/* apps/api/dist/public/"
  },
  "deploy": {
    "startCommand": "cd apps/api && node dist/index.js",
    "preDeployCommand": "pnpm db:migrate:railway:prod",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
